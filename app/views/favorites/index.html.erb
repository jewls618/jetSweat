<%= render 'layouts/header' %>

<h1>Favorites</h1>

<ul>
  <% @favorites.each do |fav| %>

    <li>
      <%= link_to fav.workout.name, location_category_workout_path(1, fav.workout.category.id, fav.workout.id) %>,
      <%= fav.workout.street %>
    </li>
  <% end %>
</ul>
